# 中小学作业智能批改系统需求文档

## 1. 项目概述

### 1.1 项目背景
本项目旨在开发一个基于大模型的中小学作业自动批改系统，通过图像识别、OCR技术和人工智能大模型，实现对学生作业的自动化批改，减轻教师批改负担，提高教学效率。

### 1.2 项目目标
- 实现作业图像的自动采集和预处理
- 实现试卷题目的自动识别和切割
- 实现题目内容的OCR识别
- 实现基于规则引擎和大模型的智能批改
- 支持高并发处理，满足大规模使用需求
- 提供完整的批改结果和统计分析功能

### 1.3 目标用户
- 中小学教师：用于批改学生作业
- 学生/家长：查看作业批改结果
- 教育机构：统计分析教学数据

## 2. 功能需求

### 2.1 核心功能模块

#### 2.1.1 图像采集模块
**功能描述**：通过摄像头拍摄作业图像

**需求要点**：
- 支持笔记本自带摄像头拍摄
- 支持手机摄像头拍摄（通过移动端接入）
- 支持图片文件上传（JPG、PNG、PDF格式）
- 支持实时预览拍摄效果
- 支持拍摄参数调整（亮度、对比度等）

**输入**：摄像头视频流或图片文件
**输出**：原始作业图像

#### 2.1.2 图像预处理模块
**功能描述**：对原始图像进行校正、增强和优化

**需求要点**：
- **EXIF方向校正**：自动识别并校正手机拍摄时的图像方向
- **图像增强**：
  - 自适应直方图均衡化（CLAHE）
  - 高斯滤波去噪
  - 对比度增强
- **透视变换**：
  - 边缘检测（Canny算法）
  - 轮廓识别
  - 四点透视变换校正倾斜
- **二值化处理**：自适应阈值二值化，提高OCR识别率
- **图像压缩**：
  - 支持设置压缩质量（默认85%）
  - 限制最大尺寸（默认2048×2048）

**技术实现**：
- 使用OpenCV进行图像处理
- 支持批量处理多张图片

**输入**：原始作业图像
**输出**：预处理后的高质量图像

#### 2.1.3 试卷切题模块
**功能描述**：调用阿里云OCR API识别试卷中的题目区域

**需求要点**：
- 调用阿里云 `RecognizeEduPaperCut` API
- 自动识别题目边界框（x1, y1, x2, y2）
- 支持多种题型布局识别
- 根据切题结果裁剪单题图片
- 支持Mock模式（开发测试用）

**API参数**：
- 输入：预处理后的图像（Base64编码）
- 输出：题目区域列表
  ```json
  [
    {
      "question_no": "1",
      "x1": 50,
      "y1": 50,
      "x2": 550,
      "y2": 150
    }
  ]
  ```

**异常处理**：
- API调用失败时记录日志并重试
- 支持降级到Mock模式

#### 2.1.4 题目OCR识别模块
**功能描述**：调用阿里云OCR API识别题目内容和学生答案

**需求要点**：
- 调用阿里云 `RecognizeEduQuestionOcr` API
- 识别题目文本内容
- 识别学生手写答案
- 识别题目类型（选择题、填空题、计算题等）
- 支持手写体识别
- 支持Mock模式

**API参数**：
- 输入：单题图片（Base64编码）
- 输出：OCR识别结果
  ```json
  {
    "question_text": "计算：2 + 3 = ?",
    "student_answer": "5",
    "question_type": "计算题",
    "raw_response": {}
  }
  ```

#### 2.1.5 智能批改模块
**功能描述**：基于规则引擎和大模型对题目进行智能批改

**需求要点**：

**规则引擎（客观题）**：
- **选择题**：精确匹配答案（支持多选）
- **判断题**：识别"正确/错误"、"对/错"、"True/False"等格式
- **填空题**：精确匹配或模糊匹配答案
- **计算题**：数值计算结果比对（支持误差范围）

**大模型批改（主观题）**：
- 调用通义千问API进行批改
- 生成详细评语和解题分析
- 支持作文、简答题等主观题批改
- 支持部分给分

**批改策略**：
1. 客观题优先使用规则引擎（准确率100%）
2. 规则引擎无法处理时调用大模型
3. 主观题直接使用大模型批改
4. 大模型生成评语和分析补充规则引擎结果

**输出结果**：
```json
{
  "question_no": "1",
  "score": 10,
  "max_score": 10,
  "result": "正确",
  "comment": "计算正确，步骤清晰",
  "analysis": "该题考查基础加法运算...",
  "confidence": 1.0
}
```

#### 2.1.6 批注生成模块
**功能描述**：根据批改结果在题目图片上进行批注

**需求要点**：
- 在题目区域标注得分（如：✓ 10/10 或 ✗ 0/10）
- 标注正确/错误符号
- 添加简短评语
- 支持不同颜色的批注（正确-绿色，错误-红色）
- 生成批注后的完整试卷图片

**技术实现**：
- 使用OpenCV或PIL进行图像批注
- 支持自定义批注样式

#### 2.1.7 评分统计模块
**功能描述**：计算总分并生成统计报告

**需求要点**：
- 计算单份作业总分
- 统计正确题数、错误题数
- 计算正确率
- 支持按科目、年级统计
- 支持用户维度统计（个人历史成绩）
- 支持全局统计（系统使用情况）

### 2.2 用户管理模块

#### 2.2.1 用户注册
- 支持用户名、邮箱注册
- 密码加密存储（bcrypt）
- 支持角色区分（学生、教师、管理员）

#### 2.2.2 用户登录
- JWT Token认证
- 支持Token刷新
- 登录状态管理

#### 2.2.3 用户信息管理
- 查询用户信息
- 修改用户资料
- 账号激活/停用

### 2.3 任务管理模块

#### 2.3.1 任务状态跟踪
支持的任务状态：
- PENDING：等待处理
- PROCESSING：处理中
- PREPROCESSING：图像预处理中
- CUTTING：试卷切题中
- OCRING：OCR识别中
- GRADING：批改中
- SUCCESS：处理成功
- FAILED：处理失败

#### 2.3.2 异步任务处理
- 使用Celery进行异步任务调度
- 支持任务重试（最多3次）
- 支持任务超时控制（默认300秒）
- 任务失败时记录错误信息

### 2.4 统计分析模块

#### 2.4.1 用户统计
- 用户提交作业总数
- 平均分数统计
- 各科目成绩分布
- 正确率趋势图

#### 2.4.2 全局统计
- 系统总作业量
- 各年级作业分布
- 各科目批改情况
- 系统使用趋势

## 3. 非功能需求

### 3.1 性能需求

#### 3.1.1 高并发支持
**目标**：支持100+并发请求

**技术方案**：
- **异步框架**：FastAPI原生支持异步IO
- **异步任务队列**：Celery + Redis
  - 支持任务队列隔离
  - Worker预取配置（prefetch_multiplier=1）
  - 任务序列化（JSON格式）
- **数据库连接池**：
  - 连接池大小：5
  - 最大溢出：10
  - 连接回收时间：300秒
- **Redis缓存**：
  - 缓存OCR结果
  - 缓存标准答案
  - 缓存用户会话

#### 3.1.2 响应时间要求
- 图像上传：< 2秒
- 任务状态查询：< 500ms
- 批改结果查询：< 1秒
- 单题批改：< 5秒（含OCR + 大模型调用）
- 完整作业批改：< 60秒（视题目数量而定）

#### 3.1.3 吞吐量要求
- 单机支持：100次批改/分钟
- 分布式部署：可横向扩展Worker数量

### 3.2 可用性需求

#### 3.2.1 服务降级
- 阿里云OCR不可用时自动切换Mock模式
- 大模型不可用时仅使用规则引擎
- 数据库连接失败时返回友好错误提示

#### 3.2.2 熔断机制
- 大模型调用失败5次后触发熔断
- 熔断恢复时间：30秒
- 熔断期间降级处理

#### 3.2.3 重试机制
- Celery任务失败自动重试
- 最大重试次数：3次
- 重试间隔：5秒

### 3.3 安全需求

#### 3.3.1 认证授权
- JWT Token认证
- Token过期时间：120分钟
- Refresh Token有效期：7天
- 支持Token黑名单（注销功能）

#### 3.3.2 数据安全
- 密码加密存储（bcrypt）
- API密钥环境变量管理
- 敏感信息不记录日志
- SQL注入防护（SQLAlchemy ORM）

#### 3.3.3 文件安全
- 文件类型白名单验证
- 文件大小限制（默认10MB）
- 文件名UUID化，防止路径遍历
- 上传目录权限控制

#### 3.3.4 限流保护
- 默认限流：1000次请求/分钟
- 支持IP级别限流
- 支持用户级别限流

### 3.4 可扩展性需求

#### 3.4.1 水平扩展
- 无状态API服务，支持多实例部署
- Celery Worker可独立扩展
- Redis作为共享缓存和消息队列
- 数据库支持主从读写分离

#### 3.4.2 插件化设计
- 规则引擎可扩展新题型
- 大模型可替换（通义千问、GPT等）
- OCR服务可替换（阿里云、腾讯云等）

### 3.5 可维护性需求

#### 3.5.1 日志管理
- 使用Loguru统一日志
- 日志级别：DEBUG、INFO、WARNING、ERROR
- 日志轮转：1天
- 日志保留：30天
- 结构化日志输出

#### 3.5.2 监控告警
- 任务失败告警
- API调用失败告警
- 系统异常告警
- 性能指标监控

#### 3.5.3 配置管理
- 环境变量配置（.env文件）
- 配置热更新（部分配置）
- 配置验证（Pydantic）

## 4. 技术架构

### 4.1 技术栈

| 类别 | 技术 | 版本 | 说明 |
|------|------|------|------|
| Web框架 | FastAPI | 0.115.11 | 高性能异步框架 |
| ASGI服务器 | Uvicorn | 0.34.0 | 支持异步IO |
| 生产服务器 | Gunicorn | 23.0.0 | 多进程管理 |
| 异步任务 | Celery | 5.5.1 | 分布式任务队列 |
| 消息队列 | Redis | 5.2.1 | Broker和缓存 |
| ORM | SQLAlchemy | 2.0.36 | 数据库ORM |
| 数据库 | SQLite/PostgreSQL | - | 开发/生产环境 |
| 图像处理 | OpenCV | 4.10.0.84 | 图像预处理 |
| 图像处理 | Pillow | 11.1.0 | 图像批注 |
| OCR | 阿里云OCR | 1.0.19 | 试卷切题和识别 |
| 大模型 | 通义千问 | 1.25.17 | 智能批改 |
| 认证 | python-jose | 3.3.0 | JWT Token |
| 日志 | Loguru | 0.7.3 | 日志管理 |
| 熔断 | pybreaker | 1.1.0 | 熔断器 |

### 4.2 系统架构图

```
┌─────────────────────────────────────────────────────────────┐
│                        客户端层                              │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐                   │
│  │ Web前端   │  │ 移动端    │  │ 摄像头    │                   │
│  └──────────┘  └──────────┘  └──────────┘                   │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│                      API网关层                               │
│  ┌──────────────────────────────────────────────┐          │
│  │  FastAPI (Uvicorn/Gunicorn)                  │          │
│  │  - 认证授权                                   │          │
│  │  - 限流控制                                   │          │
│  │  - 请求路由                                   │          │
│  └──────────────────────────────────────────────┘          │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│                      业务逻辑层                              │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐                 │
│  │ 图像服务  │  │ OCR服务   │  │ 批改服务  │                 │
│  └──────────┘  └──────────┘  └──────────┘                 │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐                 │
│  │ 任务服务  │  │规则引擎   │  │ 用户服务   │                 │
│  └──────────┘  └──────────┘  └──────────┘                 │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│                      异步任务层                              │
│  ┌──────────────────────────────────────────────┐          │
│  │  Celery Workers                              │          │
│  │  - 图像预处理任务                             │          │
│  │  - OCR识别任务                                │          │
│  │  - 批改任务                                   │          │
│  └──────────────────────────────────────────────┘          │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│                      数据存储层                              │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐                 │
│  │ 数据库   │  │  Redis   │  │ 文件存储 │                 │
│  │(SQLite/  │  │ - 缓存   │  │ - 图片   │                 │
│  │PostgreSQL)│  │ - 队列   │  │ - 批注   │                 │
│  └──────────┘  └──────────┘  └──────────┘                 │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│                      外部服务层                              │
│  ┌──────────┐  ┌──────────┐                               │
│  │ 阿里云OCR│  │ 通义千问 │                               │
│  └──────────┘  └──────────┘                               │
└─────────────────────────────────────────────────────────────┘
```

### 4.3 数据流程图

```
用户上传作业图片
        ↓
FastAPI接收请求 → 创建任务记录 → 返回task_id
        ↓
提交到Celery队列
        ↓
Worker取出任务
        ↓
图像预处理（EXIF校正 → 灰度化 → 边缘检测 → 透视变换 → 二值化）
        ↓
调用阿里云RecognizeEduPaperCut → 获取题目区域
        ↓
裁剪单题图片
        ↓
调用阿里云RecognizeEduQuestionOcr → 识别题目和答案
        ↓
匹配标准答案库
        ↓
规则引擎批改（客观题）或 大模型批改（主观题）
        ↓
生成批注图片
        ↓
计算总分 → 保存结果到数据库
        ↓
任务状态更新为SUCCESS
        ↓
用户查询批改结果
```

## 5. 数据模型设计

### 5.1 用户表（users）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | Integer | 主键 |
| username | String | 用户名（唯一） |
| email | String | 邮箱（唯一） |
| password_hash | String | 密码哈希 |
| full_name | String | 全名 |
| role | String | 角色（student/teacher/admin） |
| is_active | Boolean | 是否激活 |
| created_at | DateTime | 创建时间 |

### 5.2 作业任务表（homework_tasks）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | Integer | 主键 |
| task_id | String | 任务ID（UUID） |
| user_id | Integer | 用户ID（外键） |
| subject | String | 科目 |
| grade | String | 年级 |
| status | String | 任务状态 |
| total_score | Decimal | 总分 |
| error_message | String | 错误信息 |
| created_at | DateTime | 创建时间 |
| updated_at | DateTime | 更新时间 |

### 5.3 作业图片表（homework_images）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | Integer | 主键 |
| task_id | String | 任务ID（外键） |
| original_url | String | 原始图片路径 |
| processed_url | String | 预处理后图片路径 |
| annotated_url | String | 批注后图片路径 |
| created_at | DateTime | 创建时间 |

### 5.4 题目块表（question_blocks）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | Integer | 主键 |
| task_id | String | 任务ID（外键） |
| question_no | String | 题号 |
| question_image_url | String | 题目图片路径 |
| x1, y1, x2, y2 | Integer | 题目区域坐标 |
| created_at | DateTime | 创建时间 |

### 5.5 OCR结果表（ocr_results）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | Integer | 主键 |
| task_id | String | 任务ID（外键） |
| question_block_id | Integer | 题目块ID（外键） |
| question_no | String | 题号 |
| question_text | Text | 题目文本 |
| student_answer | Text | 学生答案 |
| question_type | String | 题目类型 |
| raw_response | JSON | 原始OCR响应 |
| created_at | DateTime | 创建时间 |

### 5.6 批改结果表（grading_results）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | Integer | 主键 |
| task_id | String | 任务ID（外键） |
| question_block_id | Integer | 题目块ID（外键） |
| question_no | String | 题号 |
| score | Decimal | 得分 |
| max_score | Decimal | 满分 |
| result | String | 结果（正确/错误/部分正确） |
| comment | Text | 评语 |
| analysis | Text | 解析 |
| confidence | Decimal | 置信度 |
| created_at | DateTime | 创建时间 |

### 5.7 标准答案表（standard_answers）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | Integer | 主键 |
| subject | String | 科目 |
| grade | String | 年级 |
| question_text | Text | 题目文本 |
| standard_answer | Text | 标准答案 |
| question_type | String | 题目类型 |
| max_score | Integer | 满分 |
| created_at | DateTime | 创建时间 |

## 6. API接口设计

### 6.1 用户管理接口

#### POST /api/v1/users/register
**功能**：用户注册
**请求参数**：
```json
{
  "username": "student1",
  "email": "student1@example.com",
  "password": "password123",
  "full_name": "张三",
  "role": "student"
}
```
**响应**：
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 1,
    "username": "student1",
    "email": "student1@example.com",
    "full_name": "张三",
    "role": "student",
    "is_active": true,
    "created_at": "2024-01-01T00:00:00"
  }
}
```

#### POST /api/v1/users/login
**功能**：用户登录
**请求参数**：
```json
{
  "username": "student1",
  "password": "password123"
}
```
**响应**：
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "access_token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
    "token_type": "bearer",
    "user": {
      "id": 1,
      "username": "student1"
    }
  }
}
```

### 6.2 作业管理接口

#### POST /api/v1/homework/upload
**功能**：上传作业图片
**请求参数**：
- file: 图片文件（FormData）
- subject: 科目（FormData）
- grade: 年级（FormData）
- user_id: 用户ID（FormData）

**响应**：
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "task_id": "task_abc123def456"
  }
}
```

#### GET /api/v1/homework/status/{task_id}
**功能**：查询任务状态
**响应**：
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "task_id": "task_abc123def456",
    "status": "SUCCESS",
    "created_at": "2024-01-01T00:00:00",
    "updated_at": "2024-01-01T00:01:00"
  }
}
```

#### GET /api/v1/homework/result/{task_id}
**功能**：查询批改结果
**响应**：
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "task_id": "task_abc123def456",
    "total_score": 85.5,
    "results": [
      {
        "question_no": "1",
        "score": 10,
        "max_score": 10,
        "result": "正确",
        "comment": "计算正确",
        "analysis": "该题考查基础加法运算",
        "confidence": 1.0
      }
    ],
    "created_at": "2024-01-01T00:00:00"
  }
}
```

#### GET /api/v1/homework/list
**功能**：获取作业列表
**请求参数**：
- user_id: 用户ID（可选）
- status: 任务状态（可选）
- page: 页码
- page_size: 每页数量

**响应**：
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "total": 100,
    "page": 1,
    "page_size": 10,
    "items": [
      {
        "task_id": "task_abc123",
        "subject": "数学",
        "grade": "三年级",
        "status": "SUCCESS",
        "total_score": 85.5,
        "created_at": "2024-01-01T00:00:00"
      }
    ]
  }
}
```

#### DELETE /api/v1/homework/{task_id}
**功能**：删除作业
**响应**：
```json
{
  "code": 200,
  "message": "删除成功",
  "data": null
}
```

### 6.3 统计分析接口

#### GET /api/v1/statistics/user/{user_id}
**功能**：用户统计
**响应**：
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "total_homework": 50,
    "average_score": 85.2,
    "subject_stats": {
      "数学": {"count": 20, "avg_score": 88.5},
      "语文": {"count": 15, "avg_score": 82.3},
      "英语": {"count": 15, "avg_score": 84.8}
    }
  }
}
```

#### GET /api/v1/statistics/global
**功能**：全局统计
**响应**：
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "total_homework": 1000,
    "total_users": 100,
    "grade_stats": {
      "一年级": {"count": 200, "avg_score": 85.0},
      "二年级": {"count": 180, "avg_score": 86.5}
    }
  }
}
```

## 7. 测试需求

### 7.1 单元测试
- 规则引擎各题型批改测试
- 图像预处理功能测试
- OCR结果解析测试
- 数据模型验证测试

### 7.2 集成测试
- 完整批改流程测试
- API接口测试
- 异步任务测试
- 数据库操作测试

### 7.3 性能测试
- 并发上传测试（100并发）
- 批改任务吞吐量测试
- 数据库连接池压力测试
- Redis缓存性能测试

### 7.4 测试样本
准备不同类型的测试样本：
- 不同年级的数学作业
- 不同年级的语文作业
- 不同年级的英语作业
- 包含多种题型的综合试卷
- 手写体样本
- 打印体样本
- 倾斜/模糊样本

### 7.5 测试报告
测试完成后生成报告，包含：
- 测试样本数量
- 各题型识别准确率
- 批改准确率
- 平均处理时间
- 错误类型分布
- 性能指标

## 8. 部署需求

### 8.1 开发环境
- Python 3.10+
- SQLite数据库
- Redis本地服务
- 本地文件存储

### 8.2 生产环境
- Python 3.10+
- PostgreSQL数据库（主从）
- Redis集群
- Nginx反向代理
- 分布式文件存储（OSS/NFS）
- Celery多Worker部署
- 监控系统（Prometheus + Grafana）

### 8.3 部署架构
```
                    Nginx (负载均衡)
                          ↓
        ┌─────────────────┼─────────────────┐
        ↓                 ↓                 ↓
   FastAPI实例1      FastAPI实例2      FastAPI实例3
        └─────────────────┼─────────────────┘
                          ↓
        ┌─────────────────┼─────────────────┐
        ↓                 ↓                 ↓
   Celery Worker1    Celery Worker2    Celery Worker3
        └─────────────────┼─────────────────┘
                          ↓
        ┌─────────────────┼─────────────────┐
        ↓                 ↓                 ↓
   PostgreSQL主     PostgreSQL从      Redis集群
```

## 9. 项目进度规划

### 9.1 第一阶段：基础功能开发（已完成）
- ✅ 项目框架搭建
- ✅ 用户管理模块
- ✅ 图像预处理模块
- ✅ OCR服务集成
- ✅ 规则引擎开发
- ✅ 大模型服务集成
- ✅ 异步任务系统
- ✅ API接口开发

### 9.2 第二阶段：功能完善（进行中）
- ⏳ 批注生成模块
- ⏳ 标准答案库管理
- ⏳ 统计分析完善
- ⏳ 错误处理优化
- ⏳ 日志和监控

### 9.3 第三阶段：性能优化
- ⏳ 高并发优化
- ⏳ 缓存策略优化
- ⏳ 数据库查询优化
- ⏳ 图像处理性能优化

### 9.4 第四阶段：测试和部署
- ⏳ 单元测试
- ⏳ 集成测试
- ⏳ 性能测试
- ⏳ 测试样本准备
- ⏳ 测试报告生成
- ⏳ 生产环境部署

## 10. 风险和挑战

### 10.1 技术风险
- **OCR识别准确率**：手写体识别可能存在误差
  - 解决方案：图像预处理优化、多次识别验证
- **大模型批改准确性**：主观题批改标准难以量化
  - 解决方案：结合规则引擎、人工复核机制
- **高并发性能**：大量并发可能导致服务过载
  - 解决方案：限流、队列、横向扩展

### 10.2 业务风险
- **题目类型多样性**：不同学科、不同年级题型差异大
  - 解决方案：可扩展的规则引擎、持续优化
- **标准答案维护**：需要大量标准答案数据
  - 解决方案：建立标准答案库、教师录入接口
- **批改公平性**：自动批改可能存在争议
  - 解决方案：人工复核机制、申诉渠道

### 10.3 依赖风险
- **阿里云OCR服务**：服务不可用影响核心功能
  - 解决方案：降级策略、多服务商备份
- **通义千问服务**：大模型服务稳定性
  - 解决方案：熔断机制、降级到规则引擎
- **Redis服务**：消息队列故障影响异步任务
  - 解决方案：Redis集群、持久化配置

## 11. 总结

本需求文档详细描述了中小学作业智能批改系统的功能需求、非功能需求、技术架构、数据模型、API接口、测试需求和部署需求。系统采用现代化的技术栈，支持高并发处理，具有良好的可扩展性和可维护性。

核心创新点：
1. **智能批改策略**：规则引擎 + 大模型混合批改，兼顾准确性和灵活性
2. **高并发架构**：异步任务队列 + 连接池 + 缓存，支持大规模并发
3. **服务降级**：Mock模式 + 熔断机制，保证系统可用性
4. **完整流程**：从图像采集到批注生成，全流程自动化

后续工作重点：
1. 完善批注生成功能
2. 准备测试样本并进行全面测试
3. 性能优化和压力测试
4. 生产环境部署和监控
